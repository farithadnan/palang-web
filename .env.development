# Committed defaults for `npm run dev` (Vite development mode).
# Unset here on purpose: localhost is detected as "your own copy" and every
# ?mode=hosted|third|self override is available for testing the hosted copy.
# Set a staging domain here (or in .env.development.local) when the dev
# server must speak for a real domain.
