services:
  palang:
    build: .
    ports:
      - "8000:80"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost/version.json"]
      interval: 30s
      timeout: 5s
      retries: 3
