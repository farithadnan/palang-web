# Committed per-mode defaults — production builds (npm run build / build:app).
# The official instance. Point this at your own domain to claim the
# "official instance" wording on your deploy.
VITE_HOST_URL=https://palang.oh-alam.my
