<!-- App-only builds replace the landing page with this stub (see vite.config.js
     $landing alias). It renders nothing: the app shell maps the home route
     straight to Convert, so users land in the tool, not on a promo page. -->
