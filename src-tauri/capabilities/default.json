{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window — the app is fully on-device, no plugins beyond the core.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
